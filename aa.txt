/* General Body Styling */
body {
  background: url('https://i.giphy.com/media/v1.Y2lkPTc5MGI3NjExY29ucjJ5Y3JxcndmdTdnOHFmeTA2dXI4b3hyYjAycDJmajZzenNtMCZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/xytwrhaA1JSbBtgxew/giphy.gif') no-repeat center center fixed;
  background-size: cover;
  font-family: Arial, sans-serif;
  color: white;
  text-align: center;
  margin: 0;
  padding: 0;
}

/* Game Container Styling */
.game-container {
  max-width: 600px;
  margin: auto;
  padding: 20px;
  background-color: rgba(0, 0, 0, 0.8);
  border-radius: 10px;
  box-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
}

/* Card Sections */
.cards {
  display: flex;
  justify-content: space-between;
  margin: 20px 0;
}

.card-display {
  font-size: 20px;
  color: #fff;
  display: flex;
  justify-content: center;
  gap: 10px;
}

.card {
  width: 50px;
  height: 70px;
  background-color: white;
  color: black;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 5px;
  font-size: 18px;
  font-weight: bold;
  box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.3);
}

/* Buttons Styling */
.bet-buttons button, .target-buttons button, .start-button, .reward-button {
  font-size: 16px;
  margin: 5px;
  padding: 10px 20px;
  border-radius: 5px;
  border: none;
  cursor: pointer;
}

.bet-buttons button {
  background-color: blue;
  color: white;
}

.target-buttons button {
  background-color: green;
  color: white;
}

.start-button {
  background-color: red;
  color: white;
}

.reward-button {
  background-color: yellow;
  color: black;
}

.reward-button:hover, .start-button:hover, .bet-buttons button:hover, .target-buttons button:hover {
  filter: brightness(1.2);
  transform: scale(1.05);
}

/* Free Credit Link */
#reward-link a {
  color: yellow;
  font-size: 14px;
  text-decoration: none;
}

#reward-link a:hover {
  text-decoration: underline;
}

/* Game Records Section */
#game-records {
  margin-top: 20px;
  display: grid;
  grid-template-columns: repeat(8, 1fr);
  grid-template-rows: repeat(2, 40px);
  gap: 5px;
  justify-items: center;
}

.record {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 35px;
  height: 35px;
  font-size: 14px;
  border-radius: 50%;
  font-weight: bold;
  color: white;
}

.record.p {
  background-color: blue; /* Player - P */
}

.record.b {
  background-color: red; /* Banker - B */
}

.record.t {
  background-color: green; /* Tie - T */
}

/* Responsive Design for Mobile */
@media (max-width: 768px) {
  #game-records {
    grid-template-columns: repeat(4, 1fr); /* Mobile: 4 columns */
    grid-template-rows: repeat(4, 40px);
    gap: 3px;
  }

  .record {
    width: 30px; /* Smaller circles */
    height: 30px;
    font-size: 12px; /* Smaller font */
  }

  .card {
    width: 40px; /* Smaller cards */
    height: 60px;
    font-size: 16px;
  }

  .bet-buttons button, .target-buttons button {
    font-size: 14px; /* Smaller buttons */
    padding: 8px 16px;
  }

  .start-button, .reward-button {
    font-size: 14px;
    padding: 10px 18px;
  }
}
